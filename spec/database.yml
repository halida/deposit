sqlite3:
  adapter: sqlite3
  database: deposit.sqlite3

mysql:
  adapter: mysql2
  hostname: localhost
  username: root
  password:
  database: deposit
  charset: utf8
  
postgresql:
  adapter: postgresql
  hostname: localhost
  username: postgres
  password:
  database: deposit
  encoding: utf8
